# 路标 · AI 服务配置
# 使用方法：
#   1. 把本文件复制一份并改名为 .env（和 server.py 放在同一目录）
#   2. 把下面的占位内容替换成你的真实 DeepSeek API 密钥
#      （在 platform.deepseek.com 的「API Keys」页面创建）
#   3. 保存后重新启动服务（双击「启动服务.bat」）
# 注意：.env 只保存在你本机，不会上传，也不会被网站发送到浏览器。

DEEPSEEK_API_KEY=sk-把你的密钥粘贴到这里

# 可选：指定模型。默认 deepseek-v4-flash（快、省），需要更强推理可改 deepseek-v4-pro
AI_MODEL=deepseek-v4-flash

# 可选：如果直连 OpenAI 不通（例如所在网络需要代理），填写本地代理地址。
# 常见 Clash 默认端口是 7890；V2Ray 常见 10809 / 1080。
# AI_PROXY=http://127.0.0.1:7890
