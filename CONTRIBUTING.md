# 贡献指南

感谢你愿意为「路标」贡献代码或数据。这是一个零依赖的轻量项目，保持简单是它的设计原则。

## 项目结构速览

- `index.html` — 页面结构；`styles.css` — 样式与深色模式；`js/app.js` — 全部前端代码（知识库数据 + 路径引擎 + 界面交互）
- `server.py` — 本地 AI 服务（Python 标准库，无第三方依赖）
- `guide.html` — 使用指南页

## 我可以贡献什么

### 1. 新增院校 / 岗位 / 公司数据（最欢迎）

在 `js/app.js` 顶部的 `KB` 对象中：

- **新增院校**：向 `KB.universities` 追加一条记录，参考现有字段（`id`、`country`、`name`、`en`、`rank`、`focus`、`programs`、`ielts`、`toefl`、`gpa`、`gre`、`fee`、`timeline`、`notes`、`source`；欧盟院校需带 `nation` 字段）。新增国家时同步 `KB.countries`。
- **新增岗位**：向 `KB.jobs` 追加岗位对象（参考 `swe` 的结构），并在 `index.html` 的目标岗位下拉框中同步加入选项。
- **新增公司时间线**：向 `KB.companyNotes` 追加，并在同文件 `companyNoteFor` 的关键词表中同步登记。

数据要求：

- 只收录**公开渠道可查的事实**（院校官网、教育部门、公司官网招聘页等），并注明 `source` 与信息时间；
- 不要编造录取率、薪资等具体数字；不确定处写"以官网为准"；
- 信息可能变化，README 与页面内已内置免责声明，提交数据时请附上来源。

### 2. 修复 Bug 或优化体验

- 保持原生 HTML / CSS / JavaScript，**不要引入框架或构建工具**；
- 保持中文界面与现有极简设计风格（沿用 `styles.css` 中的设计令牌与深色模式）；
- 改动后请在浏览器里自测：桌面与移动端、浅色与深色、知识库生成与 AI 生成两条链路。

### 3. 文档与翻译

- 更新 README、使用指南（`guide.html`）或其他文档。

## 提交流程

1. Fork 本仓库并创建分支：`git checkout -b feature/你的改动`；
2. 完成改动后提交，提交信息用中文简要描述，例如 `新增日本 3 所国立大学数据`；
3. 发起 Pull Request，说明改动内容与数据来源。

## 注意事项

- **严禁提交 `.env`**：其中包含真实 API 密钥，`.gitignore` 已排除，请勿强制添加；
- 提交前可用 `git status` 确认没有意外文件入内；
- 如有安全问题（如密钥泄露、漏洞），请直接联系维护者，不要公开细节。

## 许可

贡献即代表你同意你的贡献以 MIT License 授权给项目及所有使用者。
